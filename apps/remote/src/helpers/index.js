import { fn } from "../utils";

export const onDoubleClick = () => {
  // Commend below line to make it work
  fn();
};

export const onClick = () => {
  // Uncomment below line to make it work
  // fn()
};
